Determing best work performing couple from data file

Python version:
The main file is index.py. When you run it, it will expect in the console to enter the path to the data text file. 

JavaScript version:
Open index.html in your browser.

